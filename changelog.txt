alpha/0.0.4:

    - Every menu now uses a nice textbox
    - Implemented input text boxes

alpha/0.0.5:

    -made textbox generation functions more consistent
    -added function to send prints to a textfile
    -created subfolders for config, logs, and db
    -refactored CRUD functions to open and close a db connection as needed
    -added Textbox.Generate variant that automatically sizes a textbox based on its input
